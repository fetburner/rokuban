version: "2"

linters:
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
    - gofmt
    - goimports
    - misspell
    - unconvert
    - unparam
    - nilerr
    - errorlint

  settings:
    goimports:
      local-prefixes:
        - github.com/fetburner/rokuban
    errorlint:
      errorf: true
      asserts: true
      comparison: true
