version: "2"

linters:
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
    - gofmt
    - misspell
    - unconvert
    - unparam
    - nilerr
    - errorlint

  settings:
    errorlint:
      errorf: true
      asserts: true
      comparison: true
