> [frontend.md](../frontend.md) の一部。索引から辿る

# ホーム

## 何を解く画面か

起動して最初に見えるのが番組表（「これから録るもの」）だと、運用が安定した
録画サーバーへの再訪の大半（「録れているか」「今夜なにが録れるか」「見るもの
はあるか」「異常はないか」の確認）に 1 画面で答えられない。部品（録画中の状態・
予約一覧・サーキットブレーカーバナー・容量バッジ・ドロップ統計）は揃っていた
のに、それらを集約する面が無かった。ホーム（`/`）はその集約面で、**新しい API
は作らず**既存のエンドポイントの組み合わせだけで作る。

## ルート: `/` はホーム、番組表は `/programs`

`/` を新設のホームに譲り、番組表はここに移設した。一等地（`/`）を新しい起点に
割り当て、番組表はナビの 2 番目に置く。

**裸の `/` はホームへ、`?serviceId=` か `?at=` が付いた `/` だけ `/programs` へ
リダイレクトする**（`routes.tsx` の `homeRoute` の `beforeLoad`）。この 2 つは
番組表固有のクエリで、`/` が番組表だった頃に外部（共有・ブラウザ履歴）へ
出た URL（容量不足バッジの `?at=`・ライブ「この局の番組表」の `?serviceId=`）を
救うためのものであり、値の形の検証はしない（有無だけを見る --- 形の検証は
`/programs` 側の `validateSearch` の仕事のままにする）。裸の `/` は「番組表の
ブックマーク」と「ホームを開きたい」が原理的に区別できないので、区別できる
場合（番組表固有のクエリを持つ URL）だけを救い、それ以外は新しい意味（ホーム）
を優先する。リポジトリ内の発行元（`capacity-shortfall-badge.tsx` /
`pages/live.tsx`）は `/programs` に直したので、リダイレクトが実際に効くのは
外部に残った旧リンクだけになる。

サーバー側は変更不要 --- `internal/api/spa.go` は `/api/` 以外を index.html に
フォールバックするので、`/programs` は既に配れる。

## ナビ: ホームが一等地の先頭、予約は「その他」へ

`navItems` の先頭に「ホーム」を足す。並び・畳み方の全体は
[shell.md](shell.md) §項目の並びは頻度順が権威。ここでは要点だけ:
ボトムタブの常時項目（`MOBILE_PRIMARY_COUNT`）は 3 のまま変えず、
ホーム・番組・録画の 3 個に置き換わる。予約はホームの新設で常時タブから
「その他」へ戻った（今夜これで録れているかの確認はホーム自身が担うため）。

## セクション: 窓・上限・空のときの扱い

| セクション | データ | 窓 / 上限 | 0 件のとき |
|---|---|---|---|
| いま録画中 | `GET /api/recordings?status=recording` | 全件（同時録画数はチューナー数で上界がある） | セクションごと消す |
| 今夜〜明日の予約 | `GET /api/reservations` をクライアントで窓切り | 今から**明日の暦日の終わり**（`lib/day-offset.ts` の `dayOrigin(2, now)`。0 時基準に揃えた窓）まで / 上限 10 件、超過分は「予約をすべて見る」リンク | セクションごと消す |
| 警告 | `GET /api/breakers` + `GET /api/capacity/overages`（今夜〜明日の予約と同じ窓） + ドロップ統計（下記「ドロップ警告の検出範囲」参照） + `GET /api/recordings?status=failed`（下記「失敗録画は警告への追加項目」参照） | 発生しているものだけ | セクションごと消す |
| 直近の完了 | `GET /api/recordings?status=finished&limit=20`（警告のドロップ検出と共用。下記参照） | 表示は先頭 6 件 | セクションごと消す |

**上限 10 / 6 は実測ではなく、「1 セクションが画面を占有しない」ために置いた
恣意的な上限。** 窓の方（今夜〜明日の予約の終端）は既存の日境界（番組表の
`dayOrigin`）に揃えたという根拠がある。

### ドロップ警告の検出範囲は「直近の完了」の表示件数から独立させる（クエリは 1 本）

警告セクションのドロップ検出の範囲（`DROP_WARNING_SCAN_LIMIT` = 20）は、表示用の
「直近の完了」の件数（`RECENT_FINISHED_LIMIT` = 6）とは**独立した定数**にする。
表示件数（レイアウトの都合）と検出範囲（正しさの都合）を同じ値に乗せると、
レイアウト都合で表示件数を下げただけで警告の遡り幅まで黙って縮む。20 もまた
実測ではない恣意的な上限だが、少なくとも表示件数の変更が検出範囲に連動しない。

**問い合わせは 1 本にする**（`limit=20` で取り、表示だけを先頭 6 件へ切る）。
同じ絞り込み・同じ既定順（`program_start_at` 降順）なので `limit=6` の集合は
`limit=20` の先頭 6 件と一致し、2 本目は何も新しい情報を持ってこない。定数 2 つの
独立性はスライスでも保たれる（判定: `pages/home.test.tsx`「表示上限（6 件）の
外にある録画のドロップも警告には出る」。表示のスライスを警告の材料にも掛けると
落ちる）。

一時期は本当に 2 本のクエリに分けていた。分ける根拠として「表示件数を変える
変更と警告を壊す変更が同じ 1 行の定数変更に潰れる」と書いていたが、それは
スライスでも潰れない（定数は 2 つとも残る）ので根拠になっていなかった。
一方で払っていた代償は実在した: 問い合わせが 1 本増える（マウント時も
`recordings` の SSE invalidate のたびも 2 本走る）/ 表示ゲートが待つクエリが
1 本増える / **2 つの応答の間に録画が 1 本完了すると、同じ画面の中で表示リストと
警告の検出リストが食い違いうる**。

### 失敗録画は警告への追加項目（専用セクションは持たない）

**失敗録画（`status=failed`）はホームに専用のセクションを作らず、「警告」への
追加項目としてのみ出す。** 「直近の完了」は `status=finished` の絞り込みで
取っているため failed 行はそもそも混ざらず、警告に材料を 1 本足すだけで
既存の 4 セクション構成を変えずに済む。取る範囲は表示専用の限度を持たない
（`FAILED_RECORDING_SCAN_LIMIT`。ドロップ検出のような表示/検出の分離が要る
理由がここには無いので、値だけ他の上限と共有せず独立した定数にしてある）。

**取った失敗録画のうち、警告に出すのは recency 窓（`FAILED_RECORDING_WARNING_WINDOW_MS`。
`startAt` 基準で 7 日）の中だけにする。** 他の警告材料（ブレーカーは発動中のみ・
容量超過は今夜〜明日の窓・ドロップは「直近 20 件の完了」で実質 recency がある）は
どれも自然に消えるが、失敗だけは `FAILED_RECORDING_SCAN_LIMIT` 件に収まる限り
いつの失敗でも出続け、稼働の長いサーバーでは警告セクションが古い失敗で常時
埋まってしまう（issue の受け入れ基準も「直近の」失敗録画と言っている）。値
（7 日）は実測ではなく他の上限と同じ性質の恣意的な上限。

**行では予定尺（`durationMs`。番組の放送尺のスナップショット）と実際に録れた
尺（`startedAt`〜`endedAt`）を区別する。** 録画が開始した直後に終わった失敗
（実際は 0 分に近いのに `durationMs` は番組の予定尺のまま）を予定尺だけで
表示すると「ほぼ予定通り録れた」ように誤読できる（実機観測: 開始・終了が同じ秒
なのに表示尺は番組の予定尺だった）。`startedAt` / `endedAt` は独立に書かれる
（`UpdateRecordingStatus` は `started_at` を無条件に、`ended_at` は非 NULL の
ときだけ書く）ので 3 通りある --- 両方あり（実際尺を出す）/ `startedAt` のみ
（mirakc の failed record に `endTime` が無かった場合。開始した事実はあるが
実際尺は主張しない）/ 両方無し（**rokuban が開始を観測していない**。「未開始」と
明示する）。3 つ目で mirakc 側の事実（「mirakc が録画を開始しなかった」）は
主張しない --- `started_at` を書くのは record を観測した経路だけで
`CreateFailedRecording` は書かないので、record の観測より先に `recording.failed`
の SSE が届いた窓でも同じ形になる。3 通りの中の区切りは `・` に揃える ---
メッセージ全体が `（尺 / 理由: …）` で ` / ` を「尺と理由の境目」に使っているので、
尺の中でも使うと 1 行に 2 種類の意味のスラッシュが並ぶ。

**警告の中での並びは「今止まっている（ブレーカー）→ 既に失われた（失敗録画）→
これから足りない（チューナー不足）→ 品質が落ちた（ドロップ）」。** 失敗は
`FAILED_RECORDING_SCAN_LIMIT` 件まで並びうるので、失敗が多いと行動可能な
「チューナー不足」が下へ押し下げられる --- 件数上限は入れていない（押し下げが
実運用で問題になるかは未検証。問題になるなら上限より並びの入れ替えが先）。

**未解決: `?status=failed` は superseded 済みの failed 行を含む**（`GET
/api/recordings` は `superseded_at` を絞らず、API にも出していない）。本物の
record が観測されて置き換わった擬似 failed 行が recency 窓のあいだ警告に残り、
ユーザーには消す手段が無い。症状は 2 つの形で出る --- 実際は録れているのに
「録画失敗」と出る形と、**同じ番組が警告に 2 行並ぶ形**（本物の record も failed
だった場合。擬似行は `quality_events` を持つので「理由あり」、置き換えで作られた
行は `CreateRecording` が `quality_events` を運ばないので同じ番組が「理由不明」で
もう 1 行出る）。警告は「異常はないか」に答える場所なので、この偽陽性のコストは
一覧より高い。フロント単独では絞れないので API 側の判断が要る。

**失敗理由は `quality_events`（追記専用の履歴）の失敗系イベント（`recording.failed`
/ `recording.record-broken`）の最後の要素の `reason` があればそれを出し、無ければ
「理由不明」と沈黙を区別する。** 最後の要素そのものではなく失敗系に絞るのは、
`quality_events` に `bcas_anomaly` も混ざるため（末尾が `bcas_anomaly` だと
直前の失敗理由を読み飛ばす）。`reason` は mirakc の生の理由をそのまま保持した
もので（不変条件 7）、書き手はどちらもオブジェクト --- `recording.failed` は
`{ type, message?, osError?, exitCode? }`（`mirakc.FailedReason`。discriminated
union）を `json.Marshal` したもの、`recording.record-broken` は
`{ reason: string }`。それぞれ `type` / `reason` を読み、期待した形でない場合
（未知の `event` を含む）だけ録画単体ページの「品質イベント」欄と同じ流儀
（`JSON.stringify`）で読める形にフォールバックする。**読めたが空文字だった
場合は JSON へ落とさず「理由不明」に寄せる** --- `FailedReason.Type` に
`omitempty` は無いので `{"type":""}` はあり得る形で、それをそのまま出すと
「理由: {"type":""}」になり、材料が無い（沈黙）ことと区別できる文言にならない。

### 容量超過クエリの `start` はレンダー時刻を量子化してから渡す

**`start` に生の `Date.now()` を渡さない。** レンダーごとに変わる生ミリ秒を
クエリのパラメータ（延いては TanStack Query のキャッシュキー）に直接載せると、
レンダーのたびに新しいキーになり「未解決 → 即解決 → 再描画 → また未解決」が
閉じない無限再取得になる（下記「経緯と失敗事例」に実測値がある）。

`end`（`dayOrigin(2, now)`。日境界）は元から量子化済みで問題が無かった。
`start` も同じ関数で時境界へ量子化する（`dayOrigin(0, now)`。`pages/programs.tsx`
が「今日」の起点を作るのに使っているのと同じ関数・同じ考え方 --- **キャッシュ
キーに入る値は、答えが変わる粒度まで量子化する**という規律を、`end` 側だけに
適用して `start` 側に適用し忘れていたのがこの不具合の構造的な原因。`useRef` /
`useState` で「now を固定する」ような対症療法は採らない --- それは症状を消す
だけで、量子化の欠落という原因は残ったままになる。

量子化で窓が実際の「今」より前に広がる分（最大 59 分）は、`activeOverages`
（`pages/home.tsx`）が「実際の今より後に終わる」区間だけへクライアント側で
絞り込んで補正する。量子化はキャッシュキーの安定のためだけの手段で、表示する
内容の正しさを緩めてよい理由にはしない。`start` を時頭まで後退させて新たに
入ってくるのは「[時頭, now] に終わった区間」だけ（`openapi.yaml` の `start` は
「この時刻より後に終わる区間が対象」）なので、`endAt > now` がちょうどその分を
落とし、進行中の区間は量子化の前後で欠落しない。**この回収には両方向の判定が
ある**（`pages/home.test.tsx`「既に終わった超過区間は警告に出さない」/「時境界
より前に始まり進行中の超過区間は警告に出す」。前者はフィルタを `() => true` に
変えると落ちる）。回収が無いと「もう終わったチューナー不足」が最大 59 分ぶん
警告に出続ける。

### 時境界を越えた瞬間に警告を消さない（`keepPreviousData`）

量子化の代償として、容量超過クエリのキーは毎時 0 分に 1 回変わる。新しいキーには
まだデータが無いので、素のままだと `isPending` → 警告セクションが 1 RTT だけ消え、
**警告だけが可視だった場合（アイドルなサーバーでブレーカーだけ発動している状態）は
ページ全体が読み込みスケルトンに戻る。** この画面の主題は「セクションが理由なく
消えないこと」なので、`placeholderData: keepPreviousData`（TanStack Query）を
置いて、キーが進んでいる間は前のキーのデータを見せ続ける。

**古いデータを見せている間も、終わった区間が警告に出ることはない** —— 毎時の境界で
進むのは `start` だけなので前のキーのデータは窓が最大 1 時間だけ広い同じ問い合わせの
結果（新しい窓の上位集合）であり、そのうえで `activeOverages` が毎レンダー「今」で
絞り直す。**逆に取りこぼす側の境界は日境界にある** —— `end`（`dayOrigin(2)`）が進む
深夜 0 時には前のキーのデータが新しい窓の**部分集合**になるので、新しく窓に入った
超過が 1 RTT だけ見えない。1 RTT で自己回復するため許容するが、**この側は未検証**
（毎時の側は下記のテストで測っているが、日境界の側は測っていない）。

判定は `pages/home.test.tsx`「容量超過クエリのキーが進み、新キーが未解決のままでも
警告セクションは残る」（`placeholderData` を外すと落ちる。キーが実際に進んだことと、
**2 回目の応答が assert の時点でまだ未解決であること**もテスト内で確認している ——
後者が無いと、遅延の仕掛けが効かなくなったときに空虚な成功へ転ぶ）。

### セクションの可視性は個別に、「全部空」の判定だけ全解決を待つ

各セクションの表示/非表示はそのセクション自身のクエリ（警告は 4 本の合成 ---
サーキットブレーカー・容量超過・完了録画のドロップ検出・失敗録画）の
解決だけを待つ。`GET /api/reservations` は絞り込みを持たない全件取得なので、
予約が増えるほど応答が遅くなりうるが、それに「いま録画中」のような最も見たい
セクションまで引きずられて隠れないようにするため。

「全セクションが空」（ページ全体の単一空状態）の判定だけは全クエリの解決を
待つ --- 個別のクエリが解決する前に「空だから隠す」を判定すると、読み込み中の
一瞬を「セクションが無い」と誤読する（CLAUDE.md「非同期の空虚な成功」）。
未解決のセクションは「消えている」のではなく「まだ何も言っていない」。

**0 件のセクションは文言も出さず、セクションごと消す。** 一覧画面の空
（「条件に合う録画がありません」= 探した結果の報告）とホームの空（何も
主張しない）は意味が違う。ホームに「予約がありません」と出すと、それは
「異常なし」の裏返しの肯定的な主張になり、`docs/data.md` §6.5 の
「肯定的な文言を持たない」規律に反する。**全セクションが空のときだけ**、
ホーム全体として 1 つの空状態（「表示できる項目がありません」）を出す。

**取得の失敗は「0 件」とは区別する。** 個別のセクションのクエリが失敗した
場合、そのセクションを消さずに取得失敗を表示する --- 失敗を沈黙にすり替えると
「録れていない」のか「確認できていない」のか区別できなくなる。ただし警告
セクションの材料（サーキットブレーカー・容量超過・完了録画のドロップ検出・
失敗録画）は、他の画面（`CircuitBreakerBanner` / 予約一覧の容量バッジ）と
同じ「取得失敗は警告が無いことにする」流儀に揃えている（`docs/data.md` §6.5 が
言う「既知の盲点は警告を見逃す方向に偏っている」ことを承知のうえで、既存の
踏襲先が同じ判断をしている）。材料のうち完了録画の一覧は「直近の完了」の表示も
兼ねるので、取得が失敗すればそちら側にはエラーが出る（警告としては黙って
警告なしに縮退する）。**失敗録画は表示専用のセクションを持たない唯一の材料
なので、取得が失敗すると「失敗録画の警告が出ないこと」がどの画面にも出ない。**

### 読み込み中のレイアウトシフト（プレースホルダ）

4 本のクエリは順不同で解決するが、表示順は固定（いま録画中 → 今夜〜明日の予約
→ 警告 → 直近の完了）。**後続のセクションが先に解決して見えている状態で、
先行のセクションが後から解決すると、その節が既に描画済みの後続セクションの
上に挿し込まれて後続を下へ押す** --- Layout Instability API は「既に描画済みの
要素が動く」ことだけを CLS として数える（新しく挿し込まれた要素自身は前の位置を
持たないので、それ自体は動きとして数えない）ため、この「上に挿し込まれる」形
だけが実際に CLS を作る。末尾への追記（後ろに何も描画されていない位置に現れる）
は既存の要素を一切動かさないので無償 --- Lighthouse でホームのデスクトップに
出た要改善域の CLS は、この形だった（`web/e2e/cls.mjs`）。

対策は、**後続に既に見えているセクションがある間だけ**、まだ解決していない
先行セクションに行数ぶんのプレースホルダを先に描いておくこと --- 実データが
届いたときは「無 → 実データの高さ」ではなく「プレースホルダ → 実データの
高さ」の差分に縮む。後続に何も見えていない間はプレースホルダを出さない（末尾
への追記のまま無償にしておく）。

**プレースホルダに見出し（`<h2>`）は含めない。** 見出しの文言・`role="heading"`
は「このセクションに言うことがある」という主張そのもので、まだ解決していない
間に先出しすると「0 件のセクションは文言も出さず消す」という既存の意味が壊れ、
見出しの出現を「解決した」目印として使うテストの待ち合わせも崩れる（実際に
壊れた。`pages/home.test.tsx`「容量超過クエリのキーが進み…」）。行だけの
プレースホルダなら、見出しは以前と同じく実データの解決と同時にしか現れない。

**セクションの可視性そのものは変えない**（上記「セクションの可視性は個別に」）
--- ここで足したのはプレースホルダの表示だけで、実データはこれまでと同じく
そのセクション自身のクエリが解決するまで出さない。

## スコープ外

- ストレージ残高タイル（[recordings.md](recordings.md) の `StorageBalance`）は
  ホームに置かない。録画一覧側の関心事として独立させたままにする。
- ingest 進捗はホームに出さない。`Recording.ingest`（[recordings.md](recordings.md)
  §取り込み中であることを画面に出す）は録画一覧・録画単体ページにあり、ホームの
  「いま録画中」は `status=recording` に絞っているため、そもそも取り込みが始まって
  いない録画しか並ばない（表示すべき状態が無い）。取り込み中の録画を載せたく
  なったらセクションの絞り込み自体を変える話になるので、そのときに決める。

## 経緯と失敗事例

- ホームの新設・ルート移設・セクション構成は M8-3（issue #242）で決定。
  着手前の並走の可能性（M6-3 の番組表 `?serviceId=` との衝突）は、M6-3 が
  closed 済みだったため実際には発生しなかった（着手宣言コメント参照）。
- **容量超過クエリの `start` に生の `Date.now()` を渡し、無限再取得を作った
  （レビューで発見。実装 PR のレビュー）。** 実測: stub が即答する構成で 4 秒間に
  `/api/capacity/overages` が 37 回（クエリ文字列がすべて異なる）、stub に
  150ms の遅延を入れた実サーバー相当の構成では 4 秒間ずっと全画面スケルトンの
  まま一度も収束しなかった。**この不具合は `pnpm test`（jsdom、`vi.setSystemTime`
  で時計を固定）でも `e2e/design.mjs`（実 Chromium、`page.clock.setFixedTime`
  で時計を固定）でも検出できなかった** --- 両方とも時計を止めているため、
  「時計が動くことで露呈する」欠陥を原理的に見られない構成だった。「40 ショット
  0 NG」は当時この不具合について何も保証していなかった（CLAUDE.md「測っていない
  挙動を断言しない」に該当する失敗 --- 実ブラウザで測ったつもりで、測れない
  構成で測っていた）。直したあとは `dayOrigin(0, now)` で時境界へ量子化してから
  キーに渡す（上記「容量超過クエリの `start` は…」）。判定は時計を止めない経路を
  新設して足した（`pages/home.test.tsx` の「実時計でのクエリキー安定性」、
  `e2e/design.mjs` の同名チェック。`web/e2e/README.md` §判定を足すときの規律も
  参照）。
- **その修正自体に、同じ構造の欠陥が付いていた（次のレビューで発覚）。**
  量子化の代償を回収する `activeOverages` の 1 行に判定が 1 つも無く、
  `filter((o) => new Date(o.endAt).getTime() > nowMs)` を `filter(() => true)` に
  変えても当時の 17 件全部が緑だった。**1 巡目は「実ブラウザで測ったつもりで、
  測れない構成で測っていた」、2 巡目は「量子化の代償は回収したとこの doc に
  書いたが、回収を測っていなかった」** --- 直したことと、直ったことを測ったことは
  別物である。両方向の判定を足して、フィルタ除去の変異で実際に落ちることを
  確認した（上記「容量超過クエリの `start` は…」の末尾）。
- **新設した「実時計」の判定 2 本（jsdom / 実ブラウザ）が上限しか持たず、下限が
  無かった**（同じレビュー）。「要求回数 ≤ N」だけを見る判定は、クエリを消した・
  `enabled: false` にした・そもそもページが起動しない、のいずれでも 0 回で緑に
  なる。とくに `e2e/design.mjs` 側はホームの目印を待たずに数え始めていた。
  下限（`>= 1`）と、数え始める前の目印待ちを足した（`enabled: false` にする変異で
  両方が落ちることを確認済み）。
- **「表示用」と「ドロップ検出用」で `GET /api/recordings?status=finished` を
  2 回叩いていたのを 1 回に統合した**（同じレビュー）。分ける根拠として書いていた
  「定数が潰れる」はスライスでも成立するので根拠になっておらず、代償（リクエスト
  増・表示ゲートの 1 本増・SSE invalidate ごとに 2 本・2 応答間で表示と検出が
  食い違う）だけが実在した（上記「ドロップ警告の検出範囲は…」）。
- **毎時 0 分にキーが変わる瞬間、警告セクションが 1 RTT だけ消えていた**
  （同じレビュー）。無限再取得（毎秒数回）から毎時 1 回に落ちてはいたが、
  「セクションが理由なく消えないこと」がこの画面の主題なので
  `placeholderData: keepPreviousData` を置いて閉じた（上記「時境界を越えた瞬間に
  警告を消さない」）。
